# Binance Demo Trading only (simulated funds). NEVER commit real secrets.
# Create keys in Binance → Demo Trading UI (production keys will fail on demo-api).

BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_BASE_URL=https://demo-api.binance.com
